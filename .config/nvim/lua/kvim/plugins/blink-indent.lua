return {
    'saghen/blink.indent',
    config = function()
        require('blink.indent').setup({
            -- static = {
            --     char = '.'
            -- },
            -- scope = {
            --     char = '.'
            -- }
        })
    end,
}
